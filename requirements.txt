# Core
anthropic>=0.40.0
python-dotenv>=1.0.0
pyyaml>=6.0
chromadb>=0.5.0
yt-dlp>=2024.1.0
croniter>=2.0.0

# Voice (optional - not needed for text mode)
openai-whisper>=20240930
kokoro @ git+https://github.com/hexgrad/kokoro.git@dfb907a02bba8152ca444717ca5d78747ccb4bec
sounddevice
PyAudio==0.2.14
numpy>=1.24
torch>=2.2.0

# Test
pytest>=8.0

# Wake word
# Pinned to 0.4.0: 0.5.0+ requires tflite-runtime, which has no PyPI wheel
# for Python 3.12+ on ARM/x86. 0.4.0 uses ONNX (already a transitive dep)
# and ships the prebuilt model files bundled in the package.
openwakeword==0.4.0

# Voice activity detection
silero-vad>=5.0

# Faster CPU Whisper (Wave 3)
faster-whisper>=1.0

# Faster Kokoro on Pi 5 — ONNX Runtime int8 quantized variant of the same
# 82M Kokoro model. Runs ~2-3x faster than PyTorch on ARM64 CPU which is
# enough to push synth-vs-realtime ratio below 1.0, eliminating the
# between-sentence gaps that the parallel pipeline alone can't close.
kokoro-onnx>=0.5.0

# Spotify Web API client (used by core/spotify_auth.py + skills/spotify/)
spotipy>=2.24

# ElevenLabs TTS SDK
elevenlabs
